export const splitToArray = num => num.toString().split("");
export const sortArray = array => array.sort();
export const findNextLargerCombination = (numArray, sortedArray) => {
  let nextLarger = [];
  for (let i = 0, len = array.length; i < len; i++) {
    if (sortedArray[i] >= numArray[0]) {
    }
  }
};

function nextBigger(n) {}
export default nextBigger;
